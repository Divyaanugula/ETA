version: '3.8'

services:
  raileta:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: raileta_app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped
